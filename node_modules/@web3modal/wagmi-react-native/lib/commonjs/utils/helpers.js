"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.caipNetworkIdToNumber = caipNetworkIdToNumber;
exports.getCaipDefaultChain = getCaipDefaultChain;
exports.getCaipTokens = getCaipTokens;
var _constants = require("./constants");
var _presets = require("./presets");
function getCaipDefaultChain(chain) {
  if (!chain) {
    return undefined;
  }
  return {
    id: `${_constants.NAMESPACE}:${chain.id}`,
    name: chain.name,
    imageId: _presets.NetworkImageIds[chain.id]
  };
}
function getCaipTokens(tokens) {
  if (!tokens) {
    return undefined;
  }
  const caipTokens = {};
  Object.entries(tokens).forEach(_ref => {
    let [id, token] = _ref;
    caipTokens[`${_constants.NAMESPACE}:${id}`] = token;
  });
  return caipTokens;
}
function caipNetworkIdToNumber(caipnetworkId) {
  return caipnetworkId ? Number(caipnetworkId.split(':')[1]) : undefined;
}
//# sourceMappingURL=helpers.js.map