import { Animated, Pressable } from 'react-native';
import { Icon } from '../../components/wui-icon';
import useAnimatedValue from '../../hooks/useAnimatedValue';
import { useTheme } from '../../hooks/useTheme';
import styles from './styles';
const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
export function IconLink(_ref) {
  let {
    icon,
    onPress,
    size = 'md',
    iconColor = 'fg-100',
    backgroundColor,
    pressedColor = 'gray-glass-010',
    disabled,
    style
  } = _ref;
  const Theme = useTheme();
  const bgColor = backgroundColor ? Theme[backgroundColor] : 'transparent';
  const {
    animatedValue,
    setStartValue,
    setEndValue
  } = useAnimatedValue(bgColor, Theme[pressedColor]);
  return /*#__PURE__*/React.createElement(AnimatedPressable, {
    onPress: onPress,
    onPressIn: setEndValue,
    onPressOut: setStartValue,
    disabled: disabled,
    style: [styles.container, styles[`container-${size}`], {
      backgroundColor: animatedValue
    }, style]
  }, /*#__PURE__*/React.createElement(Icon, {
    name: icon,
    size: size,
    color: disabled ? 'gray-glass-020' : iconColor
  }));
}
//# sourceMappingURL=index.js.map