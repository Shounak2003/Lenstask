import { useSnapshot } from 'valtio';
import { ModalController } from '@web3modal/core-react-native';
import { ConnectButton } from '@web3modal/ui-react-native';
export function W3mConnectButton(_ref) {
  let {
    label,
    loadingLabel,
    size = 'md',
    style
  } = _ref;
  const {
    open
  } = useSnapshot(ModalController.state);
  return /*#__PURE__*/React.createElement(ConnectButton, {
    onPress: () => ModalController.open(),
    size: size,
    loading: open,
    style: style
  }, open ? loadingLabel : label);
}
//# sourceMappingURL=index.js.map