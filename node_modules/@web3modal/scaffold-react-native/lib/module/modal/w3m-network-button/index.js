import { useSnapshot } from 'valtio';
import { AccountController, ApiController, AssetUtil, ModalController, NetworkController } from '@web3modal/core-react-native';
import { NetworkButton } from '@web3modal/ui-react-native';
export function W3mNetworkButton(_ref) {
  let {
    disabled,
    style
  } = _ref;
  const {
    isConnected
  } = useSnapshot(AccountController.state);
  const {
    caipNetwork
  } = useSnapshot(NetworkController.state);
  return /*#__PURE__*/React.createElement(NetworkButton, {
    imageSrc: AssetUtil.getNetworkImage(caipNetwork),
    imageHeaders: ApiController._getApiHeaders(),
    disabled: disabled,
    variant: "shade",
    style: style,
    onPress: () => ModalController.open({
      view: 'Networks'
    })
  }, caipNetwork?.name ?? (isConnected ? 'Unknown Network' : 'Select Network'));
}
//# sourceMappingURL=index.js.map