import { useSnapshot } from 'valtio';
import { W3mAccountButton } from '../w3m-account-button';
import { W3mConnectButton } from '../w3m-connect-button';
import { AccountController } from '@web3modal/core-react-native';
export function W3mButton(_ref) {
  let {
    balance,
    disabled,
    size,
    label = 'Connect',
    loadingLabel = 'Connecting',
    accountStyle,
    connectStyle
  } = _ref;
  const {
    isConnected
  } = useSnapshot(AccountController.state);
  return isConnected ? /*#__PURE__*/React.createElement(W3mAccountButton, {
    style: accountStyle,
    balance: balance,
    disabled: disabled
  }) : /*#__PURE__*/React.createElement(W3mConnectButton, {
    style: connectStyle,
    size: size,
    label: label,
    loadingLabel: loadingLabel
  });
}
//# sourceMappingURL=index.js.map